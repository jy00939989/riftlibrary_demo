# 2026-07-07 工作日志

## 项目诊断

### Fable 5 深度分析（3轮）
- **R1**：喂 Project Brief → 9 维度全量诊断。P0 叙事诊断：10 位角色逐人审视，每位给出 Voice Anchor + 重写优先级 Top 3（王小磊/程远/沈明远）
- **R2**：喂夏蝉全部 41 条原文 → 逐条四档诊断（保留/调整/重写/删句）+ 完整重写范例。核心判断：「骨架及格线以上，裂缝才是夏蝉最贵的部分」。「感叹号不是预算问题，是动态范围为零」
- **R3**：数值平衡（3.5）+ 游戏性（A）+ 可玩度（B）+ 可行度（C）+ 商业化（D）+ 运维（E）完整诊断

关键结论：
- 6 本假书（PEIZHOU_BOOKS）是残次品，需要替换
- 纯前端架构足够，永久放弃 Godot，路线定为 Web → Tauri(Steam) → Capacitor(移动)
- 商业化：PWYW + Patreon 月更角色订阅
- 智慧之光通胀需要消耗出口（位面盘缠/馆藏修复/声望捐赠）

### 代码架构审计（自审 + 架构师审核）
- 制定了 `js/core/` 纯函数层抽取方案
- 经架构师审核，修正 6 处函数签名/export 状态不准确、3 处副作用拆分设计细化
- 确认方案可行后执行

---

## 代码改动

### 第一期技术改动（对应用户可见）
| 功能 | 文件 |
|------|------|
| 存档导出/导入（.json 下载 + 剪贴板 + 备份恢复） | `js/save-manager.js`（新） |
| 全局错误兜底（崩溃恢复面板，三个选项：刷新/重置/导出备份） | `js/app.js` |
| 加载进度条（parchment 风格，分段进度文字） | `index.html` + `js/app.js` |

### Bug 修复
| Bug | 根因 | 修复 |
|-----|------|------|
| 首次专注无音效 | `initSfx()` 在 `handleCompleteFocus()` 之后才被调用 | `onFirstInteraction()` 提前到 `handleStartFocus()` 开头 + 全局 click 兜底 |
| 图南寄语反复弹出 | `ensureGuideQuests` ↔ `tryCompleteAllDone` ↔ `getCurrentQuest` 死循环 | 重写 q10 补全逻辑为内联版本 |
| 加载进度条卡死在"正在点亮烛台" | init() 异常导致终点 `hideLoadingScreen()` 未执行 | try-finally 包裹初始化代码 |

### 叙事重写
| 角色 | 改动 |
|------|------|
| **夏蝉** 常层 base ×6 | 全部重写 — 按"先算账再打气"铁律，感叹号从每句 3-4 个降到 0-2 个 |
| **夏蝉** 常层 expand1 ×4 | 全部重写 — 带头删掉标语型感叹号 |
| **夏蝉** 常层 expand2 ×4 | 全部重写 — 保留最好的两条（《书架间的空地》/翻第一本歌词本） |
| **夏蝉** 偶层 ×5 | 全部重写 — xc_o01 从荧光笔标语变成"算了四天账" |
| **夏蝉** 稀层 | 整体降调重写，"继续。再来一遍。"用句号收尾 |
| **夏蝉** 终局 | 微调 — 删口号三连，让墨墨"听到了"收尾 |
| **夏蝉** 终局后常层 ×4 | #3/#4 轻修 |
| **夏蝉** 终局后偶层 ×3 | xc_po03 删总结句 |
| **夏蝉** 还书语录 ×6 | 全部按"先算账再打气"重写 |
| **夏蝉** 氛围见证 ×4 | 全部重写，每段加具体物件/数字 |
| **visitor-events.js 顶部** | 新增写作铁律注释（5 条，防 AI 改写回潮） |

### 裴舟事件系统重写
**删除**：
- `PEIZHOU_BOOKS` — 6 本假书常量（❌ 无章节/无内容/不能誊抄）
- `buySalesBook()` — 创建 `sale_xxx` 幽灵书的函数
- 还书弹窗中的"购买/不买了"按钮

**新增 A — 荐书折扣**（50%概率）：
- 从 `SHARED_POOL` 挑真书
- `peizhouRec` state 字段存储推荐（含 24h 过期）
- 商店卡片显示"裴舟推荐"徽章 + 折后价
- 购买时自动叠加折扣

**新增 B — 随手补笔**（50%概率）：
- 从手稿箱随机挑一本未抄完的书 +3% 进度
- 手稿箱空了才走纯叙事

### 代码架构：js/core/ 纯函数层
| 新文件 | 行数 | 内容 |
|--------|------|------|
| `js/core/economy.js` | ~130 | 定价/概率/容量计算（17 函数） |
| `js/core/visitor-lookup.js` | ~140 | 10 人光环加成/语录/见证 |
| `js/core/book-utils.js` | ~90 | 章节进度/书籍状态/工厂函数 |
| `js/core/curation.js` | ~70 | 策展连携（纯搬运） |
| `js/core/achievement-stats.js` | ~70 | 成就统计/加成计算/todayKey |

15 个私有函数提升为 export（visitors.js ×4, quests.js ×3, app.js ×2, achievements.js ×6）

### 细节优化
- 缮写室钢笔图标 `🖋️`/`✒️` → 羽毛笔 `🪶`（4 处替换）

---

## 文档产出
- `docs/FABLE5_PROJECT_BRIEF.md` — 喂给 Fable 5 的完整项目简报
- `docs/FABLE5_ROUND1_DIAGNOSIS.md` — 三轮诊断完整归档
- `docs/FABLE5_ROUND2_XIACHAN.md` — 夏蝉原文集（喂给 Fable 5 R2）
- `docs/plans/core-extraction-plan.md` — 纯函数层抽取方案（经架构师审核修正）

---

## 改动统计
| 类型 | 文件数 | 净增行 |
|------|--------|--------|
| 新增 | 7 | ~1,200 |
| 修改 | 15 | ~800 改 |
| 删除 | — | ~120（假书 + buySalesBook） |
| 文档 | 4 | ~2,000 |
