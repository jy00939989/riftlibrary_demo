# 书库系统重设计 · 讨论纪要

日期：2026-05-10

---

## 一、废除项

| 废除项 | 原因 |
|--------|------|
| 稀有度系统（common/rare/epic/legendary） | 书籍无数值收益，稀有度无意义；有文化风险（评判文学价值） |
| 专注项目选择器（深度学习/工作事务/创意创作） | 标注的加成效果从未实现，且稀有书概率已不适用 |
| 誊抄选择区展示全量可抄书 | 改为从书架启动，专注页只放已开启过的书 |
| `tryUnlockNewBook()` 函数 | 死代码，从未被调用 |

---

## 二、新书获取途径（三途径）

### 途径1：代币商店
- **固定区**：初始上架5本，买走 → 24小时后补货
- **轮换区**：3本特价书（折扣价）
- **定价**：统一价 500~800 代币（依字数微调），福利特价 100~300
- **未解锁书籍不占位**

### 途径2：累计字数里程碑
- 按 `state.focus.totalWords` 累计，到达阈值随机送一本
- 书源：与商店共享混合池
- 解锁方式：纯随机
- 具体阈值待定

### 途径3：访客好感度专属书
- 每位访客有好感度系统
- 好感度增长：来馆阅览 + 借书 + 还书收取
- 好感度达标 → 该访客的专属书加入其随机事件池 → 还书时概率赠书
- 沈明远赠书改为好感度专属池
- 阿九推销池与商店/里程碑共享混合池（非阿九专属书）

### 书源架构
```
├── 访客专属池（好感度驱动）
│   ├── 沈明远专属
│   ├── 小萤专属
│   ├── 云游专属
│   └── 阿九专属
│
├── 商店/里程碑混合池（共享）
│   ├── 商店购买
│   └── 里程碑随机送
│
└── 阿九推销（特殊）
    └── 还书事件 → 从混合池随机推销
```

---

## 三、重复抄写系统（Mastery 1-5）

| 等级 | 名称 | 解锁内容 |
|------|------|----------|
| Lv1 | 初识 | 书籍上架，可被借阅 |
| Lv2 | 熟悉 | 作者小传 |
| Lv3 | 精通 | 创作轶闻 |
| Lv4 | 大师 | 名家书评 |
| Lv5 | 传承 | 典藏封面 · 金色光效 |

- 纯收集向，无功能性收益
- 每本书需新增4段文本字段：`authorBio`、`anecdotes`、`reviews`、`collectorCover`
- master 5 的书从专注页快捷选择区移出

---

## 四、书架系统

### 扩容
- 每个书架固定容量（初始5格）
- 扩容方式：买新书架
- 价格递增但有封顶，具体数值后续测试调优

### 检索管理
- **保持书架视觉**，不改为列表
- **标签筛选**：状态（全部/誊抄中/已完成）、分类、字数排序
- **星标收藏**：每本书可加星，筛选栏有「我的收藏」
- Demo 不做拖拽整理（标记为后续债务）

---

## 五、书籍分类

- 单层文学体裁枚举
- 固定值：`童话 | 寓言 | 小说 | 诗歌 | 戏剧 | 散文 | 哲学 | 传记 | 历史 | 科学 | 神话 | 志怪`
- 新增书籍必须从已有分类中选择
- 访客偏好匹配沿用现有逻辑，无需改动

---

## 六、专注页改动

### 移除
- 项目选择器（深度学习/工作事务/创意创作）

### 誊抄选择改为快捷启动
- 仅展示 `copiedWords > 0` 且 `masteryLevel < 5` 的书
- 换书从书架页面启动

### 新增完成结算卡片
- 每次专注结束弹出，展示：
  - 专注时长
  - 誊抄字数
  - 获得代币
  - 一句书籍名言（当前书 quotes 中随机，无书则用通用鼓励句）

---

## 七、商店与补货

- 未解锁书籍不在书架占位
- 补货：买走后24小时自动补货
- 轮换刷新周期待定

---

## 八、实现状态（2026-05-10）

### ✅ 已实现

| 模块 | 内容 |
|------|------|
| 架构重构 | `data/books.js` 拆分为入口 + 独立书文件；`js/render.js` 拆分为 `js/render/` 9个模块 |
| 稀有度移除 | 数据 + CSS + 渲染全部清理 |
| 项目选择器移除 | `state.js` 3处同步删除 |
| 分类枚举化 | `CATEGORIES` 常量 12个值 |
| mastery 内容字段 | `authorBio`, `anecdotes`, `reviews`, `collectorCover` |
| 氛围初始值 | 改为 0 |
| 收藏系统 | `starred` 字段 + 书架筛选 + 星标切换 |
| 专注页改造 | 快捷启动（copiedWords>0 && mastery<5）+ 书架「开始誊抄」 |
| 结算卡片 | `showCompletionCard` 专注完成后弹出 |
| 弹窗优先级链 | `handlePostFocusEffects()` 串联：完成→解锁→里程碑→结算 |
| 书架筛选 | 状态/分类/字数/收藏四维筛选 |
| mastery 详情弹窗 | 五级进度 + 解锁内容阅览 + 典藏金光 |
| 书架扩容 | `300 × 2^(n-1)` 封顶 4800，按钮动态显示价格 |
| 里程碑系统 | 7个阈值（5万~120万）+ 自动检测 + 弹窗奖励 |
| 访客好感度 | 浏览+1 / 借书+3 / 还书+5 + `visitorFavors` 全局累计 + 旧存档迁移 |
| 死代码清理 | 删除 `js/books.js`, `js/atmosphere.js`, `js/render.js` |
| 架构文档 | `ARCHITECTURE.md` |

### ⏳ 待实现

- 完整商店系统（固定5本 + 轮换3本 + 24h补货 + `data/book_pool.js` 共享池）
- 访客专属书池（`data/visitor_books.js`）
- 好感度门槛触发专属赠书
- 沈明远/阿九书池改造
- 动态生成的书籍缺少章节内容（需接入 BOOKS 数据）
- 书库拖拽整理
- 数值平衡测试调优
